
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fio-kata-emptydir
spec:
  selector:
    matchLabels:
      app: fio-kata-emptydir
  replicas: 1
  template:
    metadata:
      labels:
        app: fio-kata-emptydir
    spec:
      runtimeClassName: kata
      containers:
      - name: fio
        image: mauke53/fio:latest
        command: [ "/bin/sh", "-c", "--" ]
        args: [ "while true; do sleep 30; done;" ]
        ports:
        - containerPort: 80
        volumeMounts:
        - mountPath: /emptydir
          name: emptydir-volume
      volumes:
        - name: emptydir-volume
          emptyDir:
            medium: Memory
            sizeLimit: "4Gi"
